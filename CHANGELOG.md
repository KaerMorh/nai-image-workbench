# Changelog

本项目使用语义化版本编号。

## [0.6.6] - 2026-08-23

### Changed

- 新增常驻队列状态条，折叠和展开时均可查看生成、等待、重试、暂停及关闭状态；点击状态条可双向折叠面板。
- 新安装默认折叠面板，并将 History 保存状态标识默认关闭。
- 折叠 / 展开按钮固定宽度，切换符号时不再挤压相邻按钮。

## [0.6.5] - 2026-08-22

### Changed

- 普通提示默认停留时间改为 2 秒，入队按钮改用 RGB `112, 119, 194`，并增加标签名称与数量之间的间距。
- 批量替换使用固定 Seed 且存在相邻重复单位时，启动前允许选择继续执行或返回修改列表。

## [0.6.4] - 2026-08-22

### Changed

- 队列和批量模式不再生成或改写 Seed，执行与重试均保留 NovelAI 原始请求参数。
- 入队按钮立即调用 NovelAI 原始流程校验并截住通过校验的请求；原站拦截后不会创建任务或暂停整个队列。
- 设置窗口底部新增作者署名，并新增仅监听本机地址的开发脚本服务。

## [0.6.3] - 2026-08-22

### Changed

- 将设置窗口拆分为“队列设置”和“使用体验”两组，并把窗口标题简化为“设置”。
- 面板顶部按钮改为“队列启用 / 队列禁用”，与设置中的等待队列开关实时同步。
- 默认通知位置改为右上角，History 保存状态标识改为默认开启。
- 折叠面板时只隐藏内容区域，不再缩窄面板宽度。

## [0.6.2] - 2026-08-22

### Changed

- 将公开名称、用户脚本文件名、开发加载器和内部标识统一为 NAI Image Workbench。
- 重写 README，使项目页聚焦核心卖点、安装方式和最重要的运行限制。
- 新增完整使用说明、开发文档、贡献指南和 MIT License。
- 为队列演示、队列面板和批量替换面板预留图片位置。

## [0.6.1] - 2026-08-22

### Changed

- 将 History 保存状态从整圈边框改为右下角状态标识。
- 减少 History 状态更新时的重复属性写入。

### Fixed

- 修复开启 History 保存状态后无法正常切换图片的问题。
- 状态标识不再接收指针事件。

## [0.6.0] - 2026-08-22

### Added

- 新增批量 Prompt 替换模式。
- 支持 `{{变量}}` 占位符和逐行替换列表。
- 支持暂停、继续、重试、跳过和重新读取配置。
- 支持冻结 Image2Image、Vibe Transfer 和 Precise Reference。
- 新增 History 保存状态和通知位置设置。
